# File generated by github.com/posener/goaction. DO NOT EDIT.

name: main
inputs:
  port:
    default: 8080
    description: "Port to listen to"
    required: false
  addr:
    default: http://localhost
    description: "Address that the server is mounted on."
    required: false
  config:
    default: config.json
    description: "Config file to load."
    required: false
runs:
  using: docker
  image: Dockerfile
  args:
  - "-port=${{ inputs.port }}"
  - "-addr=${{ inputs.addr }}"
  - "-config=${{ inputs.config }}"
